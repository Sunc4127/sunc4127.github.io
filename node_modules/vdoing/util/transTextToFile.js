const fs = require('fs')

module.exports = function transTextToFile(rootPath, fileList) {
  console.log(fileList)
  fileList.forEach((fileInfo, index) => {
    const { name, mdText } = fileInfo
    const fileOrderName = index + '.' + name;
    fs.writeFileSync(rootPath + name,  mdText)
  });
}